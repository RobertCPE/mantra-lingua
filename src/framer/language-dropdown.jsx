// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project 826eadcfe2264c6d "Mantra Lingua", do not edit manually */
"use client";

/**
 * @typedef Locale
 * 'yue'
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
 * cMSValues?: string // CMS Values
 * placeholder?: string // Placeholder
 * defaultSelection?: string // Default Selection
 * background?: string // Background
 * textColor?: string // Text Color
 * borderColor?: string // Border Color
 * hoverColor?: string // Hover Color
 * selectedColor?: string // Selected Color
 * checkmarkColor?: string // Checkmark Color
 * font?: undefined // Font
 * borderRadius?: number // Border Radius
 * maxDropdownHeight?: number // Max Dropdown Height
 * dropdownIcon?: React.ReactNode // Dropdown Icon
}} Props

 */
import { routes } from "./chunks/chunk-74LWP5VC.js";

// virtual:language-dropdown
import { Fragment } from "react";
import { ContextProviders } from "unframer";

// /:https://framerusercontent.com/modules/Y7FICBP5RsXnpbaNeyjr/SEqYXVChO1WWdAJMR3V9/languageDropdown.js
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useState, useMemo, useRef, useEffect, startTransition } from "react";
import { addPropertyControls, ControlType } from "unframer";
function LanguageDropdown(props) {
	const {
		cmsValues = "English, Spanish, French, German, Italian, Portuguese, Dutch, Russian, Chinese, Japanese",
		placeholder = "Please choose language 1",
		selectedValue = "",
		backgroundColor = "#FFFFFF",
		textColor = "#000000",
		borderColor = "#CCCCCC",
		hoverColor = "#F5F5F5",
		selectedColor = "#E8F4FD",
		checkmarkColor = "#000000",
		font,
		borderRadius = 8,
		maxHeight = 300,
		dropdownIcon,
	} = props;
	const [isOpen, setIsOpen] = useState(false);
	const [selected, setSelected] = useState(selectedValue);
	const [hoveredIndex, setHoveredIndex] = useState(-1);
	const dropdownRef = useRef(null);
	const options = useMemo(() => {
		return cmsValues
			.split(",")
			.map((item) => item.trim())
			.filter((item) => item.length > 0);
	}, [cmsValues]);
	useEffect(() => {
		const handleClickOutside = (event) => {
			if (dropdownRef.current && !dropdownRef.current.contains(event.target)) {
				startTransition(() => setIsOpen(false));
			}
		};
		if (typeof document !== "undefined") {
			document.addEventListener("mousedown", handleClickOutside);
			return () =>
				document.removeEventListener("mousedown", handleClickOutside);
		}
	}, []);
	const handleToggle = () => {
		startTransition(() => setIsOpen(!isOpen));
	};
	const handleSelect = (option, index) => {
		startTransition(() => {
			setSelected(option);
			setIsOpen(false);
			setHoveredIndex(-1);
		});
	};
	const handleKeyDown = (event) => {
		if (!isOpen) {
			if (event.key === "Enter" || event.key === " ") {
				event.preventDefault();
				startTransition(() => setIsOpen(true));
			}
			return;
		}
		switch (event.key) {
			case "Escape":
				startTransition(() => setIsOpen(false));
				break;
			case "ArrowDown":
				event.preventDefault();
				startTransition(() => {
					setHoveredIndex((prev) => (prev < options.length - 1 ? prev + 1 : 0));
				});
				break;
			case "ArrowUp":
				event.preventDefault();
				startTransition(() => {
					setHoveredIndex((prev) => (prev > 0 ? prev - 1 : options.length - 1));
				});
				break;
			case "Enter":
				event.preventDefault();
				if (hoveredIndex >= 0) {
					handleSelect(options[hoveredIndex], hoveredIndex);
				}
				break;
		}
	};
	const displayText = selected || placeholder;
	const hasSelection = selected.length > 0;
	return (
		<div
			ref={dropdownRef}
			style={{
				position: "relative",
				width: "100%",
				...props.style,
			}}
		>
			<button
				onClick={handleToggle}
				onKeyDown={handleKeyDown}
				style={{
					width: "100%",
					padding: "12px 16px",
					backgroundColor,
					color: textColor,
					border: `1px solid ${borderColor}`,
					borderRadius: `${borderRadius}px`,
					cursor: "pointer",
					display: "flex",
					alignItems: "center",
					justifyContent: "space-between",
					outline: "none",
					transition: "border-color 0.2s ease",
					...font,
				}}
				aria-expanded={isOpen}
				aria-haspopup={"listbox"}
				role={"combobox"}
			>
				<span
					style={{
						display: "flex",
						alignItems: "center",
					}}
				>
					{hasSelection && (
						<svg
							width={"16"}
							height={"16"}
							viewBox={"0 0 24 24"}
							fill={"none"}
							stroke={checkmarkColor}
							strokeWidth={"2"}
							strokeLinecap={"round"}
							strokeLinejoin={"round"}
							style={{
								marginRight: "8px",
							}}
						>
							<polyline points={"20,6 9,17 4,12"} />
						</svg>
					)}
					{displayText}
				</span>
				<div
					style={{
						width: "16px",
						height: "16px",
						transform: isOpen ? "rotate(180deg)" : "rotate(0deg)",
						transition: "transform 0.2s ease",
						display: "flex",
						alignItems: "center",
						justifyContent: "center",
						pointerEvents: "none",
					}}
				>
					{dropdownIcon}
				</div>
			</button>
			{isOpen && (
				<div
					style={{
						position: "absolute",
						top: "100%",
						left: 0,
						right: 0,
						backgroundColor,
						border: `1px solid ${borderColor}`,
						borderRadius: `${borderRadius}px`,
						boxShadow: "0 4px 12px rgba(0, 0, 0, 0.1)",
						zIndex: 9999,
						maxHeight: `${maxHeight}px`,
						overflowY: "auto",
						marginTop: "4px",
					}}
					role={"listbox"}
				>
					{options.map((option, index) => {
						const isSelected = option === selected;
						const isHovered = index === hoveredIndex;
						return (
							<div
								onClick={() => handleSelect(option, index)}
								onMouseEnter={() =>
									startTransition(() => setHoveredIndex(index))
								}
								onMouseLeave={() => startTransition(() => setHoveredIndex(-1))}
								style={{
									padding: "12px 16px",
									cursor: "pointer",
									backgroundColor: isSelected
										? selectedColor
										: isHovered
											? hoverColor
											: "transparent",
									color: textColor,
									display: "flex",
									alignItems: "center",
									transition: "background-color 0.15s ease",
									...font,
								}}
								role={"option"}
								aria-selected={isSelected}
							>
								{isSelected && (
									<svg
										width={"16"}
										height={"16"}
										viewBox={"0 0 24 24"}
										fill={"none"}
										stroke={checkmarkColor}
										strokeWidth={"2"}
										strokeLinecap={"round"}
										strokeLinejoin={"round"}
										style={{
											marginRight: "8px",
										}}
									>
										<polyline points={"20,6 9,17 4,12"} />
									</svg>
								)}
								{option}
							</div>
						);
					})}
				</div>
			)}
		</div>
	);
}
addPropertyControls(LanguageDropdown, {
	cmsValues: {
		type: ControlType.String,
		title: "CMS Values",
		defaultValue:
			"English, Spanish, French, German, Italian, Portuguese, Dutch, Russian, Chinese, Japanese",
		displayTextArea: true,
		placeholder: "Enter comma-separated values...",
	},
	placeholder: {
		type: ControlType.String,
		title: "Placeholder",
		defaultValue: "Please choose language 1",
	},
	selectedValue: {
		type: ControlType.String,
		title: "Default Selection",
		defaultValue: "",
		placeholder: "Leave empty for no default selection",
	},
	backgroundColor: {
		type: ControlType.Color,
		title: "Background",
		defaultValue: "#FFFFFF",
	},
	textColor: {
		type: ControlType.Color,
		title: "Text Color",
		defaultValue: "#000000",
	},
	borderColor: {
		type: ControlType.Color,
		title: "Border Color",
		defaultValue: "#CCCCCC",
	},
	hoverColor: {
		type: ControlType.Color,
		title: "Hover Color",
		defaultValue: "#F5F5F5",
	},
	selectedColor: {
		type: ControlType.Color,
		title: "Selected Color",
		defaultValue: "#E8F4FD",
	},
	checkmarkColor: {
		type: ControlType.Color,
		title: "Checkmark Color",
		defaultValue: "#000000",
	},
	font: {
		type: ControlType.Font,
		title: "Font",
		defaultValue: {
			fontSize: "15px",
			variant: "Medium",
			letterSpacing: "-0.01em",
			lineHeight: "1.3em",
		},
		controls: "extended",
		defaultFontType: "sans-serif",
	},
	borderRadius: {
		type: ControlType.Number,
		title: "Border Radius",
		defaultValue: 8,
		min: 0,
		max: 20,
		step: 1,
		unit: "px",
	},
	maxHeight: {
		type: ControlType.Number,
		title: "Max Dropdown Height",
		defaultValue: 300,
		min: 150,
		max: 500,
		step: 10,
		unit: "px",
	},
	dropdownIcon: {
		type: ControlType.ComponentInstance,
		title: "Dropdown Icon",
	},
});

// virtual:language-dropdown
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [
	{
		id: "fpVdyfuAw",
		name: "Chinese",
		slug: "yue",
		code: "yue-Hant-HK",
	},
];
var defaultResponsiveVariants = {};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"826eadcfe2264c6d2229fd04fa5eb333deb0eadb9ffdc700c653a9ec57b56ae2"
			}
			locale={locale}
			locales={locales}
		>
			<LanguageDropdown {...rest} />
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders LanguageDropdownFramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"826eadcfe2264c6d2229fd04fa5eb333deb0eadb9ffdc700c653a9ec57b56ae2"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={LanguageDropdown}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, LanguageDropdown);
var language_dropdown_default = ComponentWithRoot;
export { language_dropdown_default as default };
